let express,dotenv;_c71‍.w("express",[["default",["express"],function(v){express=v}]]);_c71‍.w("dotenv",[["default",["dotenv"],function(v){dotenv=v}]]);


dotenv.config();

const app = express();
app.use(express.json());

app.get("/", (req, res) => {
  res.send("Hello World!");
});
PORT = process.env.PORT;

app.listen(PORT, () => {
  console.log("Server is running on port 5000");
});
